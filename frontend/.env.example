# Base URL of the deployed API Gateway stage (Lane A, todo.md Task 2).
# Leave unset for local work: the client then serves the committed fixtures and the UI
# labels them DEMO FIXTURE, per the Live -> Cached -> Demo Fixture ladder in PRD §7.2.
VITE_API_BASE_URL=
